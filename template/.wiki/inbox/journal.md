# Inbox journal (write-ahead log)

One line per "might be wiki-grade" moment, appended the instant it occurs.
Format: `- [YYYY-MM-DD] <one-line pointer>`. Promote qualifying lines into
wiki pages at the next natural pause, then delete them. See
`schema/AGENTS.llm-wiki.md` → "Compaction boundary rule".
